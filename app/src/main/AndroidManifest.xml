<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carrental">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".worker.finish.FinishActivity"></activity>
        <activity android:name=".worker.income.IncomeActivity" />
        <activity android:name=".worker.review.ReviewActivity" />
        <activity android:name=".worker.workerInfo.WorkerInfoActivity" />
        <activity android:name=".worker.customer.CustomerInfoActivity" />
        <activity android:name=".worker.car.CarInfoActivity" />
        <activity android:name=".login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".customer.CustomerActivity" />
        <activity android:name=".worker.WorkerActivity" />
    </application>

</manifest>